{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Git Push Current File",
            "type": "shell",
            "command": "git add \"${file}\"; git commit -m \"Update ${fileBasename}\"; git push",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}